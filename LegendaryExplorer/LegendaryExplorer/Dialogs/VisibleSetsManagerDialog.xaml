<bases:TrackingNotifyPropertyChangedWindowBase x:Class="LegendaryExplorer.Dialogs.VisibleSetsManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:bases="clr-namespace:LegendaryExplorer.SharedUI.Bases"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Visible Sets" Width="760" Height="520">
    <DockPanel Margin="10" LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBlock Text="Move classes between Visible and Hidden sets." TextWrapping="Wrap" Margin="0,0,0,6"/>
            <StackPanel Orientation="Horizontal">
                <ToggleButton Content="Add All" Click="AddAll_Click" Padding="10,3" Margin="0,0,6,0" ToolTip="Move all classes to visible"/>
                <ToggleButton Content="Clear All" Click="ClearAll_Click" Padding="10,3" ToolTip="Move all classes to hidden"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <ToggleButton Content="Clear Actor Level Settings" Click="ClearActorLevelSettings_Click" Padding="10,3" ToolTip="Remove per-actor visibility overrides and use class-level visibility only"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Click="OK_Click" IsDefault="True" Padding="20,4" Margin="0,0,5,0"/>
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" Padding="20,4"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Visible Classes" Grid.Column="0" Margin="0,0,8,0">
                <DockPanel>
                    <TextBox DockPanel.Dock="Top" Margin="6,6,6,4" Text="{Binding VisibleFilterText, UpdateSourceTrigger=PropertyChanged}"/>
                    <ListBox x:Name="VisibleClasses_ListBox" Margin="6" ItemsSource="{Binding FilteredVisibleClasses}" SelectionMode="Extended"/>
                </DockPanel>
            </GroupBox>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="4">
                <Button Content=">" Click="MoveToHidden_Click" Width="34" Margin="0,0,0,6" ToolTip="Move selected visible classes to hidden"/>
                <Button Content="&lt;" Click="MoveToVisible_Click" Width="34" ToolTip="Move selected hidden classes to visible"/>
            </StackPanel>

            <GroupBox Header="Hidden Classes" Grid.Column="2" Margin="8,0,0,0">
                <DockPanel>
                    <TextBox DockPanel.Dock="Top" Margin="6,6,6,4" Text="{Binding HiddenFilterText, UpdateSourceTrigger=PropertyChanged}"/>
                    <ListBox x:Name="HiddenClasses_ListBox" Margin="6" ItemsSource="{Binding FilteredHiddenClasses}" SelectionMode="Extended"/>
                </DockPanel>
            </GroupBox>
        </Grid>
    </DockPanel>
</bases:TrackingNotifyPropertyChangedWindowBase>
